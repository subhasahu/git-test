pipeline {
  agent any
  stages {
    stages ('build') {
       steps {
         sh 'python --version'
       }
    }
    stage ('test') {
       steps {
         sh 'python helloworld.py'
       }
    }
    stage ('deloy') {
       steps {
         sh 'python --version'
       }
    }
 }
}
